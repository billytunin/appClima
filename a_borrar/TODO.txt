- podria haber hecho unit tests, pero no llego. aclarar esto en algun lado
- deberia deshacerme del dictionary.js me parece. es un claro indicio de que es algo que se haria en el FE, no en el BE eso. La API de Apixu para el weather tiene una manera de devolverte la traduccion. Ver bien eso.


/*
let formatted_string = `
  ${dictionary.language['TEMPERATURE'][language]}: ${response.temperature}°
  ${dictionary.language['HUMIDITY'][language]}: ${response.humidity}\n 
  ${dictionary.language['CONDITION'][language]}: ${response.condition}
`;
*/

/*
let task = cron.schedule('* * * * *', function(){
  console.log('running a task every minute');
});

task.start();
*/